# ================================================================
# CineMatch — dependencias unificadas
# Instala TODO en un solo entorno: backend + ML service + frontend
# ================================================================
# Uso:
#   python -m venv .venv
#   source .venv/bin/activate   (Mac/Linux)
#   .venv\Scripts\activate      (Windows)
#   pip install -r requirements.txt

# ----------------------------------------------------------------
# Web framework (backend + ML service comparten FastAPI/Uvicorn)
# ----------------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# ----------------------------------------------------------------
# Base de datos (backend)
# ----------------------------------------------------------------
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0

# ----------------------------------------------------------------
# Autenticación JWT (backend)
# ----------------------------------------------------------------
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<5.0
python-jose[cryptography]>=3.3,<4.0

# ----------------------------------------------------------------
# HTTP client (backend llama al ML service)
# ----------------------------------------------------------------
httpx>=0.27,<1.0
requests>=2.32,<3.0

# ----------------------------------------------------------------
# ML / recomendador (ML service)
# ----------------------------------------------------------------
tensorflow>=2.18,<2.22
scikit-learn>=1.5,<1.9
numpy>=1.26,<3.0
pandas>=2.2,<3.1
pyarrow>=17,<24
scipy>=1.13,<2.0
joblib>=1.4,<2.0

# ----------------------------------------------------------------
# Frontend
# ----------------------------------------------------------------
streamlit>=1.35,<2.0

# ----------------------------------------------------------------
# Utilidades generales
# ----------------------------------------------------------------
python-dateutil>=2.9,<3.0
typing_extensions>=4.12,<5.0
pytest
httpx
